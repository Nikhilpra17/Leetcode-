class Solution {
    public int longestConsecutive(int[] nums) {
        int ans = 0 ; 
        HashSet<Integer> hs = new HashSet<>();
        
        for (int i = 0 ; i < nums.length ; i ++){
            hs.add(nums[i]);
        }
